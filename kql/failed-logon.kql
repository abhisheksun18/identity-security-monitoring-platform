// Detects failed logon attempts, which may indicate brute-force attempts
// or unathorized access attempts against monitored accounts.
Event
| where EventID == 4625
| project TimeGenerated, Computer, EventID, RenderedDescription
| order by TimeGenerated desc